This page records what features I plan to implement in the future. This is all very lose, I will prioritize this list later - nothing is final.

As I decide what items to work on next, I'll add a feature request to this repo [here](https://github.com/djfdyuruiry/cnc-new-construction-options/issues?q=state%3Aopen%20label%3Aenhancement).

## General

- Expand power meter to full height of sidebar in high resolution
- More launcher/installer features
  - Have options to choose your mode (Demo, DOS, Full)
  - Some UI config of game launch options
  - Detect OS versions and warn if not supported (macOS < 14, windows < 7 etc.)
  - Reuse graphics and sound effects from DOS installer
- Rules option to disable building ticking sound effect
- Comprehensive documentation and tools for modders
  - Including API documentation (Lua, INI, YAML etc.)
- Good mocking tools to make testing complex scenarios and mods easy
- New functionality should replace or enhance old logic, in a opt-in manner
- (standalone) Read music from Remastered Collection and load into game if installed from Steam/Some Launcher etc.
  - Would need open-source cross platform codec to stream the files for playback
  - Could be part of the launcher/installer
- (standalone) Read movies from Remastered Collection and load into game if installed from Steam/Some Launcher etc.
  - Would need open-source cross platform codec to decode video files for playback
  - Could be part of the launcher/installer
- WASM build: https://dev.to/mattconn/compiling-an-sdl2-game-to-wasm-42fj
- Support for Windows ARM and Linux ARM (requires tools as well for launcher)
  - Large runners for Linux ARM exist on GitHub Action (limited minutes per month)
  - VCPKG requires custom triplet for Linux ARM
- Use tar.gz for mac and ubuntu builds to preserve executable permissions for binaries
- GitHub pages website:
  - JS to auto-select latest release for browser OS (using GitHub API JS Client)
  - Lua script tester using WASM or JS based Lua and the Mocks
- FAQ page for common tasks (game modding, files etc. - could include where to put custom maps)
- Guide to create your own remastered mod based on NCO (add custom scenario files in Data/CCDATA and scripts files in Data/lua)

## Tiberian Dawn

- Skirmish gameplay enhancements
  - Computer players tend to run out of money too quickly in TD skirmish
  - This is partly because RA maps for Skirmish had lots of Ore
  - Solutions could include:
    - Boosted tiberium value in Multiplayer games
    - Multiple harvesters per refinery OR update AI to build more refineries
    - Handicaps for human players (AI players get 10K but humans only 8K for example)
  - Any options can be shown on multiplayer setup/skirimish setup
  - Remaster mod would have to live with changing defaults in rules INI as remaster UI is fixed
- Make new hot keys for T and D work in remaster (use the `DLL_Export_Get_Input_Key_State` function if remaster build enabled)
- Test INI based terrain and templates (was scrapped for BIN files so performance was better, but code still exists)
  - Fix any issues as this is far easier from a map editor/external tools POV
- Allow setting Map cell ownership in Scenario INI so that player walls are sellable on scenario start
- Blue tiberium
- Mission selection screen enhancements
  - Add point of conflict
  - Maybe optionally include remaster mission names
- Enhancement to choose the strategy for the AI finding a target
  - Instead of always defaulting to 'Most north unit'
  - In the case of superweapons, this is instead of 'Most valuable thing, searching from the north'
  - Could have more enhancements that remove 'cheese' strats from the game
- Persist all skirmish/lan options in conquer.ini -> `[MultiPlayer]`
  - Partially implemented in `TiberianDawnSettings` class
- Syncing rule ini files
  - Add missing sections/rules, but leave existing values alone
  - Ignore unrecognized sections/rules, just log a warning
- Limit number of `TechnoTypeClass` and `ObjectTypeClass` fields that are surfaced in Rules API
  - Could have a `ignored_base_properties` in the rules json (not rendered in INI or docs, but still in code)
  - Some of these flags only apply to certain types (Transporter for Aircraft & Units only for example)
  - Makes INI file a bit overwhelming
  - Some flags need test to see if they would even work for given types (or just removed for now)
- More than 4 icons visible on scrollbar
  - The gold standard here would be dynamic based on in-game resolution
  - Need to make save data agnostic to this (save ignoring this constraint and load adhering to the current constraint)
  - Would have to be multiples of 4 (as the strip graphics are fixed at 4 icons in height)
- Menu dialog scaling option (x1.5, x2, x4)
  - Just scale the size, keep co-ordinates the same
  - Have safeguard where you must have a certain resolution to use it
  - Could feed into resolution presets instead of fixed resolutions (x1.25 = 800x500, x1.5 = 960x800, x2 = 1280x800)
- Ability to change AI houses and colours in Skirmish menu
- Allow changing max start units for non-base/base games in rules and Skirmish menu
- Save game screenshots
- Scenario browser similar to remastered
- Dinosaur faction in Skirmish (only with Bases OFF)
- Load scenario by drag/drop OR file browser
- UI with info/lua console etc. (use that CR1 crash decomp lib)
- Randomizer mode that changes units/structures/infantry on a map OR changes rules to RNG values
- Clone some key enhancements the NCO mode [[TD] 🛠 NCO 🛠 (RULES.INI 📃 & Lua Scripting ⚙)](https://steamcommunity.com/sharedfiles/filedetails/?id=2147631402)
  - Multiple Harvesters per Refinery (skirmish fix)?
- Configuration via INI files for easy customization (authentic C&C modding experience)
  - Bring RULES.INI up to the same level as Red Alert (Mostly complete)
  - Presets for default rules that can be selected in game or in launcher
    - Vanilla, Enhanced, Enhanced /w Cheats
    - Need to ship these as rules.ini files with game and/or launcher
    - Maybe have CLI args to tell the game to load multiple rules.ini, in a certain order (loading each in sequence, using values resolved from last load as default to inherit changes)
    - Could reload rules when new preset is chosen
- More event handling using Lua scripts
  - Ability to control scenario flow from Lua scripts, similar to OpenRA
  - New scripting APIs to access more game engine functionality
- Move game logic out to Lua (makes modding certain game behavior easy)
  - Easy first candidate is having a set of scripts for levels which hve Game.Scenarios rule flags
    - Both cleans up code and gives use case for adding APIs to modify game engine stuff
  - Mapping game types and classes using LuaBridge would enable this
    - Instances of game objects passed to Lua and used to execute code in Lua instead
- More Lua APIs
  - Have design/roadmap /w sudocode examples
  - Could have DSL like impl for certain things
    - A scenario DSL for example
    - Shouldn't overcomplicate things though
  - Break up Scenario API (C++ only - Lua would map to multiple C++ APIs)
    - ScenarioTriggers
    - ScenarioTeams
    - ScenarioHouses
    - Clock (elapsed time in various units)
  - Player (convert Game API and extract functionality from scenario APIs)
    - Centered on PlayerPtr global
    - Get/Set Allies
    - Get/Set Enemies (non-allies)
    - Get antagonist (main enemy)
  - INI Reader (wrapper around existing class)
  - Map Discovery
    - Resize map
    - Get object at co-ordinates
    - Get list of co-ordinates by cell trigger name
    - Get co-ordinates by alias/list of co-ordinates by tag (these could be created here by this API and defined by a Scenario INI file)
- Consider making `InfantryAutoScatters` an enhancement rule (default ON)
- New trigger types
  - Play FMV and return to game (See https://github.com/djfdyuruiry/cnc-new-construction-options/blob/feat/td_nco-rules-port-phase-1/tiberiandawn/debug.cpp#L178)
  - Reveal new Terrain (It's possible to change the current map size without side effects, see https://github.com/djfdyuruiry/cnc-new-construction-options/blob/feat/td_nco-rules-port-phase-1/tiberiandawn/debug.cpp#L206)
  - Show text message (similar to Red Alert)
- Support playing FMVs in Radar window
- Clone fixes and some key enhancements from Nyerguds' [C&C Gold 1.06 patch project](http://nyerguds.arsaneus-design.com/cnc95upd/cc95p106/)
  - See changelog for the binary patches and the planned future patch: https://github.com/Nyerguds/CnC_Tiberian_Dawn/blob/dev/CHANGELOG.MD
  - Very far future: CNC Net support
- Enable certain debug keys always, currently required `-CHEAT` arg (Screenshots and other non game-altering features)
  - Document what they are
  - Fix any bugs
  - Separate out game-breaking keys from Quality of Life ones
  - Use either CONQUER.INI or RULES.INI to control availability
- Document existing screenshot functionality (see above)
  - Migrate from `pcx` for screenshots to `png`/`jpg` or similar
  - Could use https://wiki.libsdl.org/SDL2_image/IMG_SavePNG
- Option to use YAML files instead for modern comforts
- Make the Scenario Editor usable
- Move strings out to a config file (.ENG etc. files)
  - Use locale files
- Simple mod loading system
  - Ability to replace files gradually and add new ones, similar to original MIX file logic
- Tools/code that detects older saves and converts them to new format automatically (allows old mods and maps to be loaded)
  - May need to a custom build of Vanilla Conquer/unmodified code to preserve binary layout
  - Target v1 savegame for conversion and allow migration patching to kick in 
  - Keep tool on a branch of main repo or sub repo in an org? (cnc-save-dump)
- Implement some console version cheats - https://gamefaqs.gamespot.com/ps/7940-command-and-conquer/cheats (platform filter for more cheats)
- GODZILLA mode (Japanese audio)
- Implement planned hand-to-hand combat system for infantry
  - See `#ifdef BOXING` lines (it does compile and run with this defined)
  - There is a remastered mod that has this working
- Eeee Veee (Easter Egg)

## Red Alert

- Save/load Skirmish games (clone TD logic - special class and multiplayer variables, have at end of binary file, only process if data/flag present)
  - Remaster leverages existing 'net' save logic to capture multiplayer variables in save file
  - Similar to TD, looks like more needs captured like `Session` to properly re-hydrate a Skirmish game from a cold load
- BEER cheat mode to change ore to beer overlay tiles
- Desert theater support (port from Tiberian Dawn, example implementation: https://forums.cncnet.org/topic/7026-improved-desertmix-pal-for-ra/)

![Construction Yard Animation](img/mcv-spin.gif)

---

## Technical changes

A lot of these are behind the scenes or DevOps oriented changes, these aren't really for people who just want to play the game or build mods.

- Make some opinionated technical choices to improve DevOps experience (building/packaging)
- Slowly move select game logic out of the core engine
- Attempt to keep compatible where possible with original material
  - Old formats for maps, media files etc. should not have breaking changes
- Have util either in game engine or launch option to record logs, and use these plus runtime info to open a Github issue on behalf of the user
- Scan files in game directory on boot up and call out duplicate files with different casing (non-windows safety check) "MYMIX.MIX" mymix.mix should show a warning/error so user knows
- Migrate to MSVC 2026 when released (also means minimum supported windows will be 10 at that point)
- Migrate to SDL3
